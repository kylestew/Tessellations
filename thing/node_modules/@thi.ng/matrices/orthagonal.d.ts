import type { ReadonlyMat } from "./api.js";
/**
 * Returns true, if given square matrix of size `n` is orthagonal, i.e.
 * check if `A * AT = I`.
 *
 * {@link https://en.wikipedia.org/wiki/Orthogonal_matrix}
 *
 * @param m -
 * @param n -
 * @param eps -
 */
export declare const isOrthagonal: (m: ReadonlyMat, n: number, eps?: number) => boolean;
//# sourceMappingURL=orthagonal.d.ts.map