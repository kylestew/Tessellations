/**
 * Returns new vector of `size` with all components set to `n` (default: 0).
 *
 * @param size -
 * @param n -
 */
export declare const vecOf: (size: number, n?: number) => any[];
/**
 * Returns new one-hot vector of given `size` with `hot` element index (in
 * `[0..size)` range).
 *
 * @remarks
 * Reference: https://en.wikipedia.org/wiki/One-hot
 *
 * @param size -
 * @param hot -
 */
export declare const oneHot: (size: number, hot: number) => any[];
//# sourceMappingURL=vec-of.d.ts.map