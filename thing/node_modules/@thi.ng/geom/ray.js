import { normalize as _norm } from "@thi.ng/vectors/normalize";
import { Ray } from "./api/ray.js";
export const ray = (pos, dir, attribs, normalize = true) => new Ray(pos, normalize ? _norm(null, dir) : dir, attribs);
