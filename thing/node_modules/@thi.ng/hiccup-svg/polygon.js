import { fattribs, fpoints } from "./format.js";
export const polygon = (pts, attribs, ...body) => [
    "polygon",
    fattribs({
        ...attribs,
        points: fpoints(pts),
    }),
    ...body,
];
