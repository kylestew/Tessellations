import { mixN } from '@thi.ng/vectors/mixn';
import { set } from '@thi.ng/vectors/set';

const cubicFromQuadratic = (a, b, c) => [
    set([], a),
    mixN([], a, b, 2 / 3),
    mixN([], c, b, 2 / 3),
    set([], c),
];

export { cubicFromQuadratic };
