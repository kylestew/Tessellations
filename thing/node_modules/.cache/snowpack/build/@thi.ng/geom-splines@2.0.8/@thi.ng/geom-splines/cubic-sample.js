import { mixCubic } from '@thi.ng/vectors/mix-cubic';
import { _ as __sample, a as __sampleArray } from '../../common/sample-f92b22fa.js';
import '@thi.ng/checks/is-number';
import '@thi.ng/checks/is-plain-object';
import '@thi.ng/geom-api/sample';
import '@thi.ng/geom-resample/sampler';
import '@thi.ng/vectors/set';

const sampleCubic = __sample((res, [a, b, c, d], num) => {
    const delta = 1 / num;
    for (let t = 0; t < num; t++) {
        res.push(mixCubic([], a, b, c, d, t * delta));
    }
});
const sampleCubicArray = __sampleArray(sampleCubic);

export { sampleCubic, sampleCubicArray };
