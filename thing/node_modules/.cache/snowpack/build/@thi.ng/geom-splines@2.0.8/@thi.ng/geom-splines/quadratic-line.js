import { addmN } from '@thi.ng/vectors/addmn';
import { set } from '@thi.ng/vectors/set';

const quadraticFromLine = (a, b) => [
    set([], a),
    addmN([], a, b, 0.5),
    set([], b),
];

export { quadraticFromLine };
