import { d as defError } from '../../common/deferror-480a42fb.js';

const IllegalArityError = defError(() => "illegal arity");
const illegalArity = (n) => {
    throw new IllegalArityError(n);
};

export { IllegalArityError, illegalArity };
