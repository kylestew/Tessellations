import { d as defError } from '../../common/deferror-480a42fb.js';

const IllegalStateError = defError(() => "illegal state");
const illegalState = (msg) => {
    throw new IllegalStateError(msg);
};

export { IllegalStateError, illegalState };
