import { copyVectors } from '@thi.ng/vectors/copy';

const __copyAttribs = ($) => ({ ...$.attribs });
const __copyShape = (ctor, inst) => new ctor(copyVectors(inst.points), __copyAttribs(inst));

export { __copyAttribs as _, __copyShape as a };
